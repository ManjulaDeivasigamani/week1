package week1.day1;

public class NegativeToPositive {
	
		   public static void main(String[] args) {
		
  // Initialize an integer with a negative number
		  int negativeNum = -50; 
		  
	// Check if the number is a negative number 
		  if (negativeNum < 0);
	//convert the number to a positive number
		  int positiveNum = negativeNum+(-negativeNum*2);
	//Print as below
		  System.out.println("The number" +negativeNum+ " is coverted to +"+positiveNum);	  
		  
	}

}
